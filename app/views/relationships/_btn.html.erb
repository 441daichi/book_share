<% if current_user != user %>
  <% if current_user.following?(user) %>
    <% relationship = current_user.active_relationships.find_by(followed_id: user.id) %>
    <%= link_to "フォロー外す", user_path, method: :delete, remote: true, class: "btn btn-info" %>
  <% else %>
    <%= link_to "フォローする",  user_followings_path(user), method: :post, remote: true, class: "btn btn-success" %>
  <% end %>
<% end %>
